"""Test for my functions.

Note: because these are 'empty' functions (return None), here we just test
  that the functions execute, and return None, as expected.
"""
from my_module.functions import *

def test_get_quiz_choice():

    assert quiz_number is not None
    assert(isinstance(quiz_number, int))
    assert get_quiz_choice callable

def test_get_answer():

    assert answer is not None
    assert(isinstance(answer, int))
    assert get_answer callable
    
def test_run_facts():
    
    assert choice is not None
    assert quiz_name is not None
    assert quiz_question is not None
    assert(isinstance(quiz_name, str))
    assert surprise_message callable
    
    
    



                 
    